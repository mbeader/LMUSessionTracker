{
	"$id": "MultiplayerTeams",
	"$defs": {
		"MultiplayerDriver": {
			"$ref": "schema-MultiplayerDriver.json"
		},
		"MultiplayerTeam": {
			"$ref": "schema-MultiplayerTeam.json"
		}
	},
	"type": "object",
	"properties": {
		"coherenceId": {
			"type": "integer"
		},
		"drivers": {
			"description": "Always includes admin.",
			"type": "object",
			"patternProperties": {
				"^.+$": {
					"$ref": "#/$defs/MultiplayerDriver"
				}
			},
			"additionalProperties": false
		},
		"teams": {
			"description": "Number is slot, starting from 0.",
			"type": "object",
			"patternProperties": {
				"^utid[0-9]+$": {
					"$ref": "#/$defs/MultiplayerTeam"
				}
			},
			"additionalProperties": false
		}
	},
	"additionalProperties": false,
	"required": [
		"coherenceId",
		"drivers",
		"teams"
	]
}
